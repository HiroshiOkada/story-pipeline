# GPT-5.6 Luna 既定モデル

## 作業目標

新規作品の初期設定で利用する OpenAI モデルを `gpt-4.1` から、効率的な高頻度処理向けの `gpt-5.6-luna` へ移行する。

## 完了条件

- 新規作成される `story-pipeline-config.jsonc` が `gpt-5.6-luna` を全役割の既定モデルとして使用する。
- Chat Completions における従来の非 reasoning 相当の挙動を維持する。
- README、利用者向け文書、テストの期待値が新しい既定値と一致する。
- 全テストで回帰がないことを確認する。
