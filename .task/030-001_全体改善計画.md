# 全体改善計画

## チェックリスト

- [x] Git 競合状態の誤判定バグ(`git_validation.read_worktree` の unmerged レコードで必ず「出力形式を解釈できません」になる)を修正し、競合状態での `validate` / `run` の回帰テストを追加する。
- [x] 変更行数上限の境界ずれ(999 行で停止してしまう)を仕様どおり(1000 行以上で停止)に修正し、境界テストを追加して取扱説明書を整合させる。
- [x] `run` 実行中・終了処理のエラーで復旧案(action)と箇所(location)を表示し、終了処理失敗時は元エラーの終了コードを引き継ぐ。
- [x] LLM/API エラーを日本語の案内付きで表示し(認証失敗・接続失敗・レート制限・コンテキスト長等の kind 別定型文)、`check-llm` の FAIL 行をわかりやすくする。
- [x] 内部用語むき出しのエラーメッセージ(state キー名・stale・scaffold・Git identity・二重否定等)を言い換え、state.json 直接編集を促す仕様矛盾の復旧案を修正し、JSONC パースエラーを日本語化する。
- [x] specification.md の陳腐化した記述(python-dotenv・旧設定例・旧 state 例・check-llm 欠落・scaffold 構成)を実装に合わせ、README の誤記を修正する。
- [ ] tests/test_state.py を新設し、state 検証の不正系(重複キー・タイムスタンプ・pending_reviews 規則・番号範囲等)を網羅する。
- [ ] tests/test_config.py を新設し、config 検証の不正系(base_url scheme・api_key_env・roles 重複・禁止パラメーター等)を網羅する。
- [ ] 全テスト・compileall・配布物統合テスト・git diff --check を実行し、変更が壊れていないことを確認する。
