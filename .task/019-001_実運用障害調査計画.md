# フェーズ019 調査計画

- [x] GitHub 公開版と現在の `main` の対応を確定し、保存済み3作品から秘密を含まない時系列、状態、呼出し数、失敗分類を抽出する。
- [x] `init` 直後の `run` を API 非依存で再現し、scaffold、Git 分類、開始時コミットの契約のどこに不整合があるかを特定する。
- [x] 要求解釈の `targets` 契約を、初回作成、明示パス付き作成、継続、修正ごとに検証し、安全性を保った推測対象の扱いを決定する。
- [x] failed run 再開時の要求変更、次要求テンプレート、`request_sha256`、開始コミット、実行回数の契約を追跡し、ハッシュ不一致を再現する。
- [x] 本文候補の JSON 解析、見出し・文字数機械検査、構造化評価、知識更新抽出、evidence 完全一致の各失敗を個別に再現する。
- [ ] 採用可能な本文と知識更新抽出を独立した再開可能工程にできるか検討し、候補・評価・入出力 hash の安全な永続化案を比較する。
- [ ] トークン上限切断、HTTP 再試行、論理呼出し上限、長時間無出力、予期しない例外を分類し、秘密を漏らさずに診断と進捗を表示できる記録案を作る。
- [ ] 1章1話の全体構成で、話完了から章改稿への状態遷移、`next_chapter`、`next_episode`、完了配列を追跡し、番号矛盾と誤った次工程の原因を特定する。
- [ ] 予期しない終了コード9を、CLI 例外変換、ロック解放、run 終了処理、サブプロセス境界ごとに追跡し、再現性と診断情報の欠落を確認する。
- [ ] run JSON で呼出し別の開始・終了時刻、transport 試行回数、待機時間、入出力 token、上限切断、フォールバックを測定できるデータ契約を設計する。
- [ ] 各根本原因に対する最小再現テストを作成・実行し、調査中の仮説と結論をテスト名と失敗出力に対応付ける。
- [ ] 解決順序を「初期導線」「再開整合性」「本文成果保全」「状態遷移」「観測性と性能」の依存関係で整理し、独立にマージ可能な実装フェーズと受入条件へ分割する。
- [ ] API 非依存の回帰マトリクスと、`deepseek/deepseek-v4-flash:nitro` を使う3作品の OpenRouter 統合・性能試験の手順、上限費用、成功判定を定める。
- [ ] 調査結果、根本原因、比較した修正案、採用案、優先順位、実装フェーズ、受入条件を `.task/019-002_実運用障害調査結果と解決計画.md` に記録する。
- [ ] タスク文書の整合性、秘密非含有、差分を検査し、調査フェーズを完了する。
