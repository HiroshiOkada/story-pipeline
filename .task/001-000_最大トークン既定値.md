# 最大トークン既定値の追加

## ゴール

モデル設定に最大トークン数が記載されていない場合、128K トークンを既定値として使用する仕様へ更新する。

## 完了条件

- `max_tokens` を省略可能とし、既定値を `131072` と明記している。
- 上位仕様、詳細仕様、設定例、検証記録の記述が一致している。

## 対象外

- CLI 本体の実装
- 最大トークン数以外の設定既定値の変更
