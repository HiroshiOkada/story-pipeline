# CLI 基盤と初期化

## ゴール

詳細仕様に従うコマンドライン基盤と `init` コマンドを実装し、空のディレクトリから安全に Story Pipeline の scaffold を作成できるようにする。

## 完了条件

- `story-pipeline` のヘルプとバージョン表示が利用できる。
- `init [PATH]` が仕様どおりの scaffold を作成する。
- 不正な対象では既存内容を変更せず、規定の終了コードとエラーを返す。
- 正常系、異常系、ロールバックを自動テストで確認できる。
