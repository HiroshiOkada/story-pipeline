# リファクタリング後実LLM統合検証計画

## チェックリスト

- [ ] 構想 (`concept`) および 基礎設定 (`foundation`) ワークフローの OpenRouter 統合テストを実行し、正常動作を検証する。
- [ ] 全体構成 (`plotting`) および 話計画 (`episode_planning`) ワークフローの OpenRouter 統合テストを実行し、正常動作を検証する。
- [ ] 本文 (`drafting`)、章改稿 (`chapter_revision`)、および 全体改稿 (`final_revision`) ワークフローの OpenRouter 統合テストを実行し、正常動作を検証する。
- [ ] `run` コマンド統合テストおよび 3作品連続実行テストを実行し、パイプライン全体での連携・状態保存・成果物生成を検証する。
- [ ] 全単体テストおよび各種統合テストの結果を確認し、リファクタリング後の完全な回帰なし・正常動作を記録・確定する。
- [ ] 変更を作業ブランチへコミットし、タスク文書の完了状態を記録する。
