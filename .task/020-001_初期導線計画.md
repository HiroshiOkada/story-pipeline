# フェーズ020 初期導線計画

- [x] `init` の初期 commit 対象と Git 前提を検証し、新規・既存の空 repository で scaffold だけを commit する。
- [x] Git identity 不足、commit 失敗、想定外の index 状態を安全停止し、scaffold と利用者の Git 状態を保った復旧案内を表示する。
- [x] 要求解釈の kind 別 `targets` 契約と planner prompt を更新し、標準要求は正規化、変更系要求は明示パスを厳格検査する。
- [x] `init` 直後の fake client `run` を含む API 非依存回帰テストを追加し、I-01/I-02 characterization を正常契約へ反転する。
- [ ] 全テスト、文書整合性、差分、秘密非含有を確認し、フェーズ020を完了する。
