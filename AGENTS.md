# 作業ルール

## 対話スレッドの開始時

1. 作業ツリーと現在のブランチを確認する。
2. 未コミットの変更がある場合は、内容を確認してユーザーに示し、コミットしてよいか確認を取る。
3. ユーザーの許可を得たら変更をコミットする。許可が得られない場合は、ユーザーが作業できるようセッションを中断する。
4. 現在のブランチが `main` 以外の場合は、`main` に切り替え、現在のブランチを `main` にマージする。

## 機能開発

- 機能を追加する前に、`.task` ディレクトリへ作業目標とチェックリストを含む Markdown 文書を作成する。
- タスク文書のファイル名は `0000_内容.md` 形式にする。内容部分には日本語・英語のどちらを使ってもよい。
- `0000_内容.md` は最上位の作業目標（ゴール）を記録する。`0001_内容.md` 以降は、そのゴールに紐づくチェックリスト付きの実装計画として作成する。
- 最初の実装計画の完了後に追加開発を始める場合は、新たな開発フェーズのゴールを `1000_内容.md`、その実装計画を `1001_内容.md` 以降として作成する。
- タスク文書を作成したら、内容にふさわしい名前の作業ブランチを作成し、タスク文書を最初のコミットとして記録してから実装を始める。
- Agent はチェックリストに従って実装する。チェックリストの項目を一つ完了するごとに、対応する実装・成果物とタスク文書の完了表記（`[x]`）を同じコミットに記録する。
- テストの作成・実行・必要な確認も、チェックリスト上の独立した作業項目として扱う。テストは複数の実装項目の後にまとめて実施する構成でもよい。
- 手戻りや変更が生じた場合は、該当するタスク文書とチェックリストを更新する。実装中の軽微な変更であれば、更新内容と成果物を作業ブランチにコミットする。機能の方針・範囲自体が変わる場合は、更新内容をコミットし、その時点の変更を `main` にマージする。

## 言語

- 文書、会話、コード内の説明は原則として日本語を使う。
- Git のコミットメッセージは英語で記述する。
